import * as vscode from 'vscode';

export function activate(context: vscode.ExtensionContext) {
    let disposable = vscode.commands.registerCommand('wealthbot.analyzeCode', () => {
        vscode.window.showInformationMessage('Analyzing Wealthbot code...');
    });

    context.subscriptions.push(disposable);
}
